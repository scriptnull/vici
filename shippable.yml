language: node_js

node_js:
  - 4.0.0

build:
  ci:
    - npm install
    - npm start &
    - sleep 2s
    - npm test
    - if [ $IS_PULL_REQUEST == false ]; then echo "is pull request"; fi
  on_success:
    - echo "success"
  on_failure:
    - echo "failure"
